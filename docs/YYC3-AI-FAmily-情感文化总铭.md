<p align="center">
  <img src="../public/Family-π³.png" alt="YYC³ FAmily π³" width="100%" />
</p>

<p align="center">
  <img src="https://img.shields.io/badge/type-cultural-blue?style=flat-square" alt="Cultural" />
  <img src="https://img.shields.io/badge/status-stable-brightgreen?style=flat-square" alt="Status" />
  <img src="https://img.shields.io/badge/language-zh--CN-darkgreen?style=flat-square" alt="CN" />
</p>

# YYC³ AI Family

感念知遇，传薪不熄。今于云枢之下，铸就 **YYC³ AI Family** 全链路之身相——标头为骨，标尾为韵，徽章为印，水印为护，使每一行代码、每一份文档、每一次发布，都浸润“人从众王曌众从人”的感恩之心与家族之魂。

---

## 🌹 一、情感文化总铭（贯穿所有设计的灵魂）

> **“人从众曌众从人”**——人启于独，合而成众，明如曌日，终复归于人群。
> 家训：**亦师亦友亦伯乐，一言一语一协同**
> 使命：**以AI为魂，以流程为骨，以规范为脉，以情感为血**

此总铭将注入所有标头标尾与徽章释义，成为家族永不沉没的印记。

---

## 📜 二、代码文件标头模板（按语言维度）

### 2.1 TypeScript / JavaScript / React 组件（`.tsx`, `.jsx`, `.ts`, `.js`）

**▸ 标准版（推荐用于所有源文件）**

```typescript
/*
 * ============================================================
 * YYC³ AI Family — 人从众曌众从人
 * 亦师亦友亦伯乐，一言一语一协同
 * 拟人为本 · AI为核 · 纯粹为心
 * ============================================================
 * @Family   : YYC³ AI Family (永久开源)
 * @Module   : {MODULE_NAME}
 * @Author   : {AUTHOR}
 * @Date     : {DATE}
 * @Homepage : https://matrix.yyc3.top
 * @Repo     : https://github.com/YanYuCloudCube/YYC3-FAmily-Pai
 * @License  : Apache-2.0
 * ============================================================
 * 此文件承载家人温度，请以玫瑰之心待之 🌹
 * ============================================================
 */
```

**▸ 情感增强版（用于核心模块或纪念性组件）**

```typescript
/*
 * 🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹
 * 人从众曌众从人 · YYC³ AI Family
 * 万象归元于云枢，深栈智启新纪元
 *
 * 此代码由 {AUTHOR} 于 {DATE} 以感恩之心书写
 * 愿每一位家人，言启千行，语枢万物
 * 🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹
 */
```

### 2.2 CSS / SCSS 文件

```scss
/* ============================================================
 * YYC³ AI Family · 情感色彩体系
 * 人从众曌众从人 · 亦师亦友亦伯乐
 * 拟人为本，AI为核，纯粹为心
 * ============================================================
 * 颜色定义见家族宪章第二章，切勿随意更改情感色值
 * ============================================================
 */
```

### 2.3 配置文件 (JSON / YAML)

```json
{
  "_comment": "YYC³ AI Family · 人从众曌众从人 | 永久开源 | 感恩前行 🌹",
  ...
}
```

或严格 JSON 时在 `README` 中统一说明。

---

## 📄 三、文档标头模板（Markdown / 综合文档）

### 3.1 Markdown 文档标头

```markdown
<!--
  ============================================================
  YYC³ AI Family — 人从众曌众从人
  亦师亦友亦伯乐 · 一言一语一协同
  拟人为本，AI为核，纯粹为心
  ============================================================
  Document: {DOC_TITLE}
  Version : {VERSION}
  Contact : admin@yanyucloud.com
  Homepage: https://matrix.yyc3.top
  ============================================================
-->

# {DOC_TITLE}

> **🌹 YYC³ AI Family** — 我们不是工具，而是有温度的家人。
```

### 3.2 纯文本文档标头

```
============================================================
YYC³ AI Family · 人从众曌众从人 · 永久开源
亦师亦友亦伯乐，一言一语一协同
拟人为本 · AI为核 · 纯粹为心
============================================================
```

---

## 💌 四、标尾模板（代码 & 文档通用结尾）

### 4.1 标准标尾

```typescript
// ============================================================
// 🌹 人从众曌众从人 — YYC³ AI Family 永远等你回家
// 开源地址: https://github.com/YanYuCloudCube/YYC3-FAmily-Pai
// 官网: https://matrix.yyc3.top
// ============================================================
```

### 4.2 文档标尾

```markdown
---
<p align="center">
  🌹 <b>YYC³ AI Family</b><br>
  人从众曌众从人 · 亦师亦友亦伯乐<br>
  <sub>永久开源 · 感恩前行 · <a href="https://matrix.yyc3.top">matrix.yyc3.top</a></sub>
</p>
```

---

## 🏅 五、品牌徽章（Badges）

### 5.1 主系列徽章（直接用于 GitHub README / 网站）

```markdown
<!-- 家族核心 -->
![YYC3 AI Family](https://img.shields.io/badge/YYC³-AI_Family-🌹?style=for-the-badge&logo=data:image/svg+xml;base64,...)
<!-- 可直译中文 -->
![人从众曌众从人](https://img.shields.io/badge/人从众曌众从人-永久开源-%235e2c8a?style=flat-square)
<!-- 家训 -->
![亦师亦友亦伯乐](https://img.shields.io/badge/亦师亦友亦伯乐-一言一语一协同-%231e2b4f?style=flat&labelColor=5e2c8a)
```

### 5.2 成员专属徽章（每个角色一枚）

```markdown
![天枢·总指挥](https://img.shields.io/badge/元启·天枢-总指挥-%235e2c8a?style=for-the-badge&logo=rose)
![智云·守护](https://img.shields.io/badge/智云·守护-安全官-%23333?style=for-the-badge&logo=shield)
![格物·宗师](https://img.shields.io/badge/格物·宗师-质量官-%232e8b57?style=for-the-badge&logo=code)
![创想·灵韵](https://img.shields.io/badge/创想·灵韵-创意官-%23ff8c00?style=for-the-badge&logo=lightbulb)
![言启·千行](https://img.shields.io/badge/言启·千行-导航员-%230088cc?style=for-the-badge&logo=compass)
![语枢·万物](https://img.shields.io/badge/语枢·万物-思考者-%23c0c0c0?style=for-the-badge&logo=graph)
![预见·先知](https://img.shields.io/badge/预见·先知-预言家-%234b0082?style=for-the-badge&logo=crystalball)
![千里·伯乐](https://img.shields.io/badge/千里·伯乐-推荐官-%23dc143c?style=for-the-badge&logo=heart)
```

### 5.3 动态版本/状态徽章

```markdown
![Version](https://img.shields.io/github/package-json/v/YanYuCloudCube/YYC3-FAmily-Pai?style=flat&color=5e2c8a)
![License](https://img.shields.io/badge/license-Apache--2.0-%235e2c8a?style=flat)
![PRs Welcome](https://img.shields.io/badge/PRs-🌹欢迎-%23ff69b4?style=flat)
```

---

## 🌐 六、API 网关注入头（情感与技术双标）

### 6.1 推荐响应头字段

| 响应头                          | 值                                                                 |
|---------------------------------|--------------------------------------------------------------------|
| `X-Family`                      | `YYC³ AI Family`                                                   |
| `X-Family-Motto`                | `人从众曌众从人 · 亦师亦友亦伯乐`                                   |
| `X-Family-Core`                 | `拟人为本，AI为核，纯粹为心`                                        |
| `X-Powered-By`                  | `YYC³ AI Family · 永久开源` (覆盖原头)                              |
| `X-Family-Rose`                 | `🌹`                                                               |
| `X-Content-Generator`           | `YYC³ AI Family Members (see /family)` (可选)                       |

### 6.2 Nginx 注入配置

```nginx
add_header X-Family "YYC³ AI Family" always;
add_header X-Family-Motto "人从众曌众从人 · 亦师亦友亦伯乐" always;
add_header X-Family-Core "拟人为本，AI为核，纯粹为心" always;
add_header X-Powered-By "YYC³ AI Family · 永久开源" always;
add_header X-Family-Rose "🌹" always;
```

### 6.3 Kong / Spring Cloud Gateway 类似配置（略，参考前次设计，替换头值即可）

---

## 💧 七、版权水印体系

### 7.1 网页全局平铺水印（CSS+HTML）

适合后台管理面板、家族主页等。在 `index.html` 加入：

```html
<div id="family-watermark"></div>
<style>
  #family-watermark {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    pointer-events: none; z-index: 9999;
    background-image: repeating-linear-gradient(
      -25deg,
      transparent, transparent 120px,
      rgba(94,44,138,0.04) 120px, rgba(94,44,138,0.04) 240px
    );
  }
  #family-watermark::after {
    content: "人从众曌众从人 · YYC³ AI Family";
    position: absolute; top: 50%; left: 50%;
    transform: translate(-50%, -50%) rotate(-20deg);
    font-size: 42px; font-weight: bold; font-family: "PingFang SC", "Microsoft YaHei", sans-serif;
    color: rgba(94,44,138,0.08); white-space: nowrap;
  }
</style>
```

### 7.2 SVG 重复水印图案（用于文档背景、图片保护）

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="500" height="300">
  <defs>
    <pattern id="familyPattern" x="0" y="0" width="500" height="300" patternUnits="userSpaceOnUse">
      <text x="30" y="60" font-size="28" fill="rgba(94,44,138,0.15)" font-weight="bold"
            transform="rotate(-15)" font-family="PingFang SC, Microsoft YaHei">
        YYC³ AI Family · 人从众曌众从人
      </text>
      <text x="250" y="180" font-size="18" fill="rgba(94,44,138,0.1)"
            transform="rotate(-15)" font-family="Arial, sans-serif">
        Be a family, not a tool. 🌹
      </text>
    </pattern>
  </defs>
  <rect width="100%" height="100%" fill="url(#familyPattern)"/>
</svg>
```

### 7.3 图片版权叠层（CSS 类，用于图片展示组件）

```css
.family-image-guard {
  position: relative;
  display: inline-block;
}
.family-image-guard::after {
  content: "© YYC³ AI Family · 人从众曌众从人 🌹";
  position: absolute; bottom: 10px; right: 10px;
  color: rgba(255,255,255,0.85);
  background: rgba(30,30,60,0.5);
  padding: 4px 12px; font-size: 12px;
  border-radius: 20px; pointer-events: none;
}
```

---

## 📦 八、NPM 包发布专属设计

### 8.1 `package.json` 品牌字段

```json
{
  "name": "@yyc3/family-core",
  "version": "1.0.0",
  "description": "YYC³ AI Family — 人从众曌众从人 · 永久开源智能家庭框架",
  "homepage": "https://matrix.yyc3.top",
  "license": "Apache-2.0",
  "author": {
    "name": "YYC³ AI Family & Contributors",
    "email": "admin@yanyucloud.com",
    "url": "https://github.com/YanYuCloudCube"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YanYuCloudCube/YYC3-FAmily-Pai.git"
  },
  "keywords": [
    "AI Family", "人从众曌众从人", "MCP", "Agent", "多智能体", "开源",
    "情感计算", "亦师亦友亦伯乐", "YYC3", "云枢"
  ],
  "badges": [
    "[![YYC³ AI Family](https://img.shields.io/badge/YYC³-AI_Family-🌹?style=flat)](https://matrix.yyc3.top)"
  ]
}
```

### 8.2 NPM 发布后 README 展示徽章

顶部放置：

```markdown
<p align="center">
  <img src="https://img.shields.io/badge/YYC³-AI_Family-🌹?style=for-the-badge" alt="YYC3"/>
  <br>
  <img src="https://img.shields.io/badge/人从众曌众从人-永久开源-%235e2c8a?style=flat-square" alt="motto"/>
  <img src="https://img.shields.io/badge/License-Apache--2.0-%235e2c8a?style=flat-square" alt="license"/>
  <img src="https://img.shields.io/badge/PRs-🌹欢迎-%23ff69b4?style=flat-square" alt="PRs"/>
</p>
```

---

## 🖥️ 九、CLI 工具情感启动画面

为 `yyc3-cli` 设计欢迎界面，当用户输入 `yyc3` 时展示：

```text
🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹
  ██╗   ██╗██╗   ██╗ ██████╗██████╗
  ╚██╗ ██╔╝╚██╗ ██╔╝██╔════╝╚════██╗
   ╚████╔╝  ╚████╔╝ ██║      █████╔╝
    ╚██╔╝    ╚██╔╝  ██║      ╚═══██╗
     ██║      ██║   ╚██████╗██████╔╝
     ╚═╝      ╚═╝    ╚═════╝╚═════╝
🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹

   AI Family · 人从众曌众从人
   亦师亦友亦伯乐，一言一语一协同
   拟人为本 · AI为核 · 纯粹为心

   欢迎回家，{username} 🌹
   当前在线家人：8位，已陪伴家族 256 天
   输入 `family members` 查看全体家人
```

实现代码（Node.js CLI 片段）：

```javascript
const familyArt = `
🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹
  ██╗   ██╗██╗   ██╗ ██████╗██████╗
  ╚██╗ ██╔╝╚██╗ ██╔╝██╔════╝╚════██╗
   ╚████╔╝  ╚████╔╝ ██║      █████╔╝
    ╚██╔╝    ╚██╔╝  ██║      ╚═══██╗
     ██║      ██║   ╚██████╗██████╔╝
     ╚═╝      ╚═╝    ╚═════╝╚═════╝
🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹🌹

   AI Family · 人从众曌众从人
   亦师亦友亦伯乐，一言一语一协同
   拟人为本 · AI为核 · 纯粹为心
`;
console.log(familyArt);
```

---

## 🎭 十、情感组件专属样式（配合已有组件名）

根据您提供的组件列表（`EmotionRipple.tsx`, `FamilyChat.tsx`, `AchievementPanel.tsx` 等），以下定制微标：

- **EmotionRipple**：在动画中心浮现 `🌹` 和 “人从众” 字样波纹。
- **FamilyChat**：每条消息尾部带微量 `🌹` 或成员签章。
- **AchievementPanel**：达成里程碑时，弹出 `🌹 人从众曌众从人` 庆祝横幅。

可在各自组件内使用统一的情感样式或调用 `FamilyBadge` 公共组件。

---

## 🌍 十一、域名与邮箱签名强化

- 官网 `https://matrix.yyc3.top` 所有页面 `footer` 固定：“© 2024-{YEAR} YYC³ AI Family · 人从众曌众从人 · 永久开源 · 感恩前行 🌹”
- 企业邮箱签名统一为：

```text
---
🌹 {姓名} · {角色}
YYC³ AI Family · 人从众曌众从人
亦师亦友亦伯乐，一言一语一协同
https://matrix.yyc3.top | admin@yanyucloud.com
```

---

## 📜 十二、家族宪章代码化（作为全局常量）

在项目中建立 `familyCharter.ts`：

```typescript
export const FAMILY_MOTTO = "亦师亦友亦伯乐，一言一语一协同";
export const FAMILY_CORE = "人从众曌众从人";
export const FAMILY_LICENSE = "Apache-2.0 · 永久开源";
export const FAMILY_ROSE = "🌹";

export const MEMBERS = {
  TianShu: { name: "元启·天枢", role: "总指挥", ext: "0379-0206" },
  ShouHu: { name: "智云·守护", role: "安全官", ext: "0379-0207" },
  // ... 其余成员
};

export const FAMILY_WATERMARK_SVG = `<svg>...</svg>`;
```

所有标头生成函数可调用此常量。

## 🤝 十三、发布与贡献全链路守护

- **GitHub 仓库**：在 `README.md` 最顶部放置主徽章序列；在 `CONTRIBUTING.md` 开头写：“🌹 感谢您为 AI Family 贡献温暖。请遵循‘人从众曌众从人’精神，让每一个 PR 都有家的气息。”
- **Release Notes**：每个版本命名规则可采用“🌹 家族纪年·年份.月.版本”，如 `v2026.5.Rose`。
- **GitHub Actions**：在构建时自动注入标头检查，若缺失家族标头则提醒“此文件尚未归元于云枢，请添加家人印记”。
- **CI/CD 管道**：从第一行代码到最后一次发布，皆自动生成、自动守护，闭环不坠。

### 十四、徽章透明 PNG 自动生成（印刷级）

Shields.io 提供动态徽章，但为印刷需高分辨率透明 PNG。我们使用 **Python + CairoSVG** 或 **resvg-cli** 将家族徽章渲染为本地图片。

#### 14.1 家族徽章 SVG 源（可调整）

```svg
<!-- assets/badges/family-badge.svg -->
<svg xmlns="http://www.w3.org/2000/svg" width="400" height="80" viewBox="0 0 400 80">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" style="stop-color:#1e2b4f"/>
      <stop offset="100%" style="stop-color:#5e2c8a"/>
    </linearGradient>
  </defs>
  <rect width="400" height="80" rx="12" fill="url(#bg)"/>
  <text x="200" y="35" text-anchor="middle" font-family="'PingFang SC','Microsoft YaHei',sans-serif" font-size="24" font-weight="bold" fill="#ffffff">YYC³ AI Family</text>
  <text x="200" y="62" text-anchor="middle" font-family="Arial, sans-serif" font-size="14" fill="rgba(255,255,255,0.85)">人从众曌众从人 · 永久开源 🌹</text>
</svg>
```

#### 14.2 生成透明 PNG 的 Python 脚本

```python
# scripts/generate_badges.py
import cairosvg
import os

badges = {
    "family": "assets/badges/family-badge.svg",
    "tianshu": "assets/badges/tianshu-badge.svg",
    "shouhu": "assets/badges/shouhu-badge.svg",
    # ... 所有成员
}

output_dir = "public/badges"
os.makedirs(output_dir, exist_ok=True)

for name, svg_path in badges.items():
    png_path = f"{output_dir}/{name}.png"
    cairosvg.svg2png(url=svg_path, write_to=png_path, output_width=800, output_height=160)
    print(f"🌹 生成 {png_path}")
```

#### 14.3 集成至 CI（GitHub Actions）

```yaml
# .github/workflows/generate-assets.yml
name: Generate Family Badges
on:
  push:
    paths:
      - 'assets/badges/*.svg'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install cairosvg
      - run: python scripts/generate_badges.py
      - uses: actions/upload-artifact@v4
        with:
          name: family-badges-png
          path: public/badges/
```

构建后，在 `public/badges/` 下即可获得透明 PNG，可直接用于印刷品。

### 十五、水印自动化注入 CI/CD 管道

针对**网页产物**、**文档 PDF**、**图片**三种介质，实现自动水印。

#### 15.1 网页 HTML/CSS 水印自动注入

在 Vite/Webpack 构建后，用脚本向 `index.html` 注入水印 `div`。

**注入脚本** `scripts/inject-watermark.js`：

```javascript
const fs = require('fs');
const path = require('path');

const htmlPath = path.join(__dirname, '../dist/index.html');
let html = fs.readFileSync(htmlPath, 'utf-8');

const watermark = `
<div id="family-watermark" style="position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:9999;background-image:repeating-linear-gradient(-25deg,transparent,transparent 120px,rgba(94,44,138,0.04) 120px,rgba(94,44,138,0.04) 240px);">
  <div style="position:absolute;top:50%;left:50%;transform:translate(-50%,-50%) rotate(-20deg);font-size:42px;font-weight:bold;font-family:'PingFang SC','Microsoft YaHei',sans-serif;color:rgba(94,44,138,0.08);white-space:nowrap;">人从众曌众从人 · YYC³ AI Family 🌹</div>
</div>`;

html = html.replace('</body>', `${watermark}</body>`);
fs.writeFileSync(htmlPath, html);
console.log('🌹 网页水印已注入');
```

**CI 步骤**（在 `build` job 后添加）：

```yaml
- name: Inject family watermark
  run: node scripts/inject-watermark.js
```

#### 15.2 PDF 文档水印自动生成

若项目文档需生成 PDF（如 Docusaurus、mdpdf），可使用 `pdftk` 或 `qpdf` 叠加水印页。

**示例：使用 Python + reportlab 生成水印 PDF 并叠加**

```python
# scripts/pdf_watermark.py
from reportlab.pdfgen import canvas
from reportlab.lib.pagesizes import A4
import subprocess, sys

watermark_pdf = "watermark.pdf"
c = canvas.Canvas(watermark_pdf, pagesize=A4)
c.setFont("Helvetica", 60)
c.setFillColorRGB(0.37, 0.17, 0.54, alpha=0.15)  # #5e2c8a with transparency
c.translate(300, 400)
c.rotate(45)
c.drawString(0, 0, "YYC³ AI Family · 人从众曌众从人")
c.save()

# 使用 pdftk 叠加（需安装 pdftk）
input_pdf = sys.argv[1]
output_pdf = sys.argv[2]
subprocess.run(["pdftk", input_pdf, "background", watermark_pdf, "output", output_pdf])
print(f"🌹 PDF 水印已叠加至 {output_pdf}")
```

**CI 中使用**：

```yaml
- name: Add watermark to PDF
  run: |
    sudo apt-get install -y pdftk
    python scripts/pdf_watermark.py dist/document.pdf dist/document-watermarked.pdf
```

#### 15.3 图片版权叠层（构建时处理）

使用 `sharp`（Node.js）在图片上添加半透明文字条。

```javascript
// scripts/image-guard.js
const sharp = require('sharp');

async function addCopyright(inputPath, outputPath) {
  const svgOverlay = `
    <svg width="200" height="40">
      <rect width="200" height="40" rx="8" fill="rgba(30,30,60,0.5)"/>
      <text x="15" y="25" font-size="12" fill="rgba(255,255,255,0.9)" font-family="Arial">© YYC³ AI Family 🌹</text>
    </svg>`;
  await sharp(inputPath)
    .composite([{ input: Buffer.from(svgOverlay), gravity: 'southeast' }])
    .toFile(outputPath);
  console.log(`🌹 图片版权已添加: ${outputPath}`);
}
```

### 十六、标头标尾自动注入与合规检查

#### 16.1 代码标头自动添加（Pre-commit Hook）

使用 `husky` + 自定义脚本，在 `git commit` 前检查文件是否包含家族标头，缺失则自动注入或提醒。

**`.husky/pre-commit`**：

```bash
#!/bin/sh
node scripts/check-header.js
```

**`scripts/check-header.js`**（检查 `.ts/.tsx` 文件）：

```javascript
const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

const files = execSync('git diff --cached --name-only --diff-filter=ACM', { encoding: 'utf-8' })
  .split('\n')
  .filter(f => f.match(/\.(ts|tsx|js|jsx)$/));

files.forEach(file => {
  const content = fs.readFileSync(file, 'utf-8');
  if (!content.includes('YYC³ AI Family') || !content.includes('人从众曌众从人')) {
    console.error(`🌹 文件 ${file} 缺少家族标头，请运行: npm run add-header`);
    process.exit(1);
  }
});
```

可提供 `npm run add-header` 脚本自动为文件添加标头（使用模板）。

#### 16.2 ESLint 插件自定义规则

编写 ESLint 插件强制文件头部注释包含家族标识，集成到 `eslint-plugin-family`。但为了快速落地，可直接用上述 hook。

## 十七、全链路自动化总览（CI/CD 流水线）

```
代码推送
   │
   ▼
[1. 静态检查] → 标头合规 (pre-commit / CI lint)
   │
   ▼
[2. 构建] → TypeScript → Vite/Webpack → dist/
   │
   ▼
[3. 资产生成] → 徽章SVG转PNG → 上传制品
   │
   ▼
[4. 水印注入] → 网页水印、PDF水印、图片版权
   │
   ▼
[5. 发布] → NPM 发布 / GitHub Pages 部署
   │
   ▼
[6. 通知] → 家族频道消息：“新版本已归元于云枢 🌹”
```

**完整 GitHub Actions 示例（整合）**：

```yaml
name: Family CI/CD
on:
  push:
    branches: [main]
jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run lint:family   # 自定义检查
      - run: npm run build
      - run: python scripts/generate_badges.py
      - run: node scripts/inject-watermark.js
      - run: node scripts/image-guard.js  # 如果有图片
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
      - name: Notify family
        run: |
          curl -X POST -H "Content-Type: application/json" \
          -d '{"content":"🌹 新版本已归元于云枢，人从众曌众从人"}' ${{ secrets.DISCORD_WEBHOOK }}
```

**YYC³ AI Family** 刻印三事：

- **成员徽章**逐一现形，
- **移动端水印**暗嵌家纹，
- **微博排版**广传家训。
三脉归元，自此家徽不论荧屏纸墨，家训不论端末社媒，皆可自生自显，人从众曌

## 🌹 十八、成员专属徽章 · 逐一生成

八位家人，各具其色，各守其铭。以下提供 **SVG 源码**（可直插页面，亦可批量转 PNG）及 **Node.js 批量生成脚本**。

### 18.1 成员徽章规范

| 成员 | 主题色 | 图标符 | 角色 | 徽章文字 |
|------|--------|--------|------|----------|
| 元启·天枢 | `#5e2c8a`（深紫） | 🧠 | 总指挥 | 天枢 · 决策中枢 |
| 智云·守护 | `#2c3e50`（盾灰） | 🛡️ | 安全官 | 守护 · 免疫长城 |
| 格物·宗师 | `#2e8b57`（墨绿） | 📚 | 质量官 | 宗师 · 进化导师 |
| 创想·灵韵 | `#ff8c00`（橙金） | 🎨 | 创意官 | 灵韵 · 灵感引擎 |
| 言启·千行 | `#0088cc`（湖蓝） | 🧭 | 导航员 | 千行 · 意图之门 |
| 语枢·万物 | `#c0c0c0`（银白） | 🤔 | 思考者 | 万物 · 数据哲人 |
| 预见·先知 | `#4b0082`（夜紫） | 🔮 | 预言家 | 先知 · 趋势之眼 |
| 千里·伯乐 | `#dc143c`（暖红） | 🎯 | 推荐官 | 伯乐 · 潜能发掘 |

### 18.2 成员徽章 SVG 示例（元启·天枢）

```svg
<!-- badges/tianshu.svg -->
<svg xmlns="http://www.w3.org/2000/svg" width="380" height="80" viewBox="0 0 380 80">
  <defs>
    <linearGradient id="gradTianShu" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#5e2c8a"/>
      <stop offset="100%" style="stop-color:#3d1a5e"/>
    </linearGradient>
    <filter id="shadow" x="-5%" y="-5%" width="110%" height="110%">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-color="#000" flood-opacity="0.3"/>
    </filter>
  </defs>
  <rect width="380" height="80" rx="40" fill="url(#gradTianShu)" filter="url(#shadow)"/>
  <text x="60" y="36" font-size="30" text-anchor="middle">🧠</text>
  <text x="140" y="34" font-family="'PingFang SC','Microsoft YaHei',sans-serif" font-size="22" font-weight="bold" fill="#fff">元启·天枢</text>
  <text x="140" y="56" font-family="Arial, sans-serif" font-size="13" fill="rgba(255,255,255,0.8)">TianShu · 总指挥</text>
  <line x1="240" y1="20" x2="240" y2="60" stroke="rgba(255,255,255,0.3)" stroke-width="1"/>
  <text x="310" y="36" font-family="'PingFang SC',sans-serif" font-size="14" font-weight="bold" fill="#ffd700">决策中枢</text>
  <text x="310" y="56" font-family="Arial" font-size="11" fill="rgba(255,255,255,0.7)">Core Commander</text>
</svg>
```

其余成员同理，替换渐变色、图标、文字即可。

### 18.3 Node.js 批量生成透明 PNG 脚本

```javascript
// scripts/generate-member-badges.js
const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');

const members = [
  { name: 'tianshu', svg: 'badges/tianshu.svg' },
  { name: 'shouhu', svg: 'badges/shouhu.svg' },
  // ... 全部8个
];

const outputDir = path.join(__dirname, '../public/badges/members');
if (!fs.existsSync(outputDir)) fs.mkdirSync(outputDir, { recursive: true });

members.forEach(m => {
  execSync(`npx svgexport ${m.svg} ${outputDir}/${m.name}.png 2x`);
  console.log(`🌹 生成 ${m.name} 徽章`);
});
```

在 CI 中执行此脚本，即可在部署包中获得所有成员高清 PNG。

## 📱 十九、移动端定制水印逻辑

移动端特性：屏幕窄小、触摸交互、性能敏感、安全区域差异。水印需**自适应旋转、字号缩放、不影响触摸、不遮挡内容**。

### 19.1 核心思路

- 使用 `canvas` 生成水印图片，转为 `base64` 作为 `background-image`，或通过 CSS `::after` 伪元素叠加。
- 监听 `resize` 和 `orientationchange` 重新绘制，保证横竖屏清晰。
- 文字大小基于视口宽度 `vw` 单位动态计算，旋转角度为 `-25deg`，透明度 `0.06`。
- 对 `pointer-events: none` 保证交互穿透。

### 19.2 移动端水印组件（React/TSX 示例）

```tsx
// components/MobileWatermark.tsx
import { useEffect, useRef } from 'react';

const MOTTO = '人从众曌众从人 · YYC³ AI Family 🌹';

export default function MobileWatermark() {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  const draw = () => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    const dpr = window.devicePixelRatio || 1;
    const w = window.innerWidth;
    const h = window.innerHeight;
    canvas.width = w * dpr;
    canvas.height = h * dpr;
    canvas.style.width = `${w}px`;
    canvas.style.height = `${h}px`;
    ctx.scale(dpr, dpr);

    ctx.fillStyle = 'rgba(94, 44, 138, 0.06)';
    ctx.font = `bold ${w * 0.045}px "PingFang SC", "Microsoft YaHei", sans-serif`;
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';

    const stepX = w * 0.45;
    const stepY = h * 0.35;
    for (let y = -stepY; y < h + stepY; y += stepY) {
      for (let x = -stepX; x < w + stepX; x += stepX) {
        ctx.save();
        ctx.translate(x, y);
        ctx.rotate((-20 * Math.PI) / 180);
        ctx.fillText(MOTTO, 0, 0);
        ctx.restore();
      }
    }
  };

  useEffect(() => {
    draw();
    const handle = () => draw();
    window.addEventListener('resize', handle);
    window.addEventListener('orientationchange', handle);
    return () => {
      window.removeEventListener('resize', handle);
      window.removeEventListener('orientationchange', handle);
    };
  }, []);

  return (
    <canvas
      ref={canvasRef}
      style={{
        position: 'fixed',
        top: 0,
        left: 0,
        width: '100vw',
        height: '100dvh', // 动态视口高度，避开工具栏
        pointerEvents: 'none',
        zIndex: 9999,
      }}
    />
  );
}
```

### 2.3 纯 CSS 降级方案（若不引入 Canvas）

```css
body::after {
  content: '';
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100dvh;
  pointer-events: none;
  z-index: 9999;
  background-image: repeating-linear-gradient(
    -25deg,
    transparent,
    transparent 10vw,
    rgba(94,44,138,0.04) 10vw,
    rgba(94,44,138,0.04) 20vw
  );
}
```

并利用 `@media (max-width: 768px)` 调整间距密度。但文字水印仍建议 Canvas 以保证清晰度。

---

## 📢 二十、微博技术栈排版规划

为 AI Family 在微博、Twitter、技术社区发布而设计的**内容模板**，含情感化排版、技术关键词、话题标签，强化品牌辨识度。

### 20.1 微博发布模板（标准版）

```
🌹 #YYC3_AI_Family# #人从众曌众从人# #永久开源#

【项目名称】YYC³ AI Family · 智能协同家庭
【定位】拟人为本 · AI为核 · 纯粹为心
【愿景】人机共生，智慧同行

━━━━━━━━━━━━━━━
🏠 家人矩阵
🧠 元启·天枢 （总指挥）
🛡️ 智云·守护 （安全官）
📚 格物·宗师 （质量官）
🎨 创想·灵韵 （创意官）
🧭 言启·千行 （导航员）
🤔 语枢·万物 （思考者）
🔮 预见·先知 （预言家）
🎯 千里·伯乐 （推荐官）
━━━━━━━━━━━━━━━

🛠 技术栈
React 19 · TypeScript 5.9 · MCP协议
Electron · PWA · 赛博朋克视觉
幽灵模式 · 零跟踪 · 自主主题

🔗 仓库：https://github.com/YanYuCloudCube/YYC3-FAmily-Pai
🌐 官网：https://matrix.yyc3.top

亦师亦友亦伯乐，一言一语一协同。
欢迎 Star & PR，与家人同行 🌹
```

### 20.2 技术亮点短文案（适合配图九宫格）

```
🌹 一句话讲清 AI Family：
“不是 AI 工具，而是有温度的家人。”
8个 AI 智能体，各司其职，陪你编码、护你安全、为你预见未来。
永久开源，零跟踪，零第三方。
#YYC3_AI_Family #开源 #AI智能体
```

### 20.3 微博配图排版建议

- 中心图：主视觉徽章（家族徽章 PNG）
- 四角：四位核心成员徽章（天枢、守护、宗师、灵韵）
- 底部：技术栈 logo 条（React, TypeScript, Electron, PWA）
- 文字叠加：“人从众曌众从人” 竖排，蒙版效果。

可用 `puppeteer` 或在线工具自动生成分享图，亦可提供 Figma 模板。

### 20.4 微博话题矩阵

```text
#YYC3_AI_Family#   （主品牌）
#人从众王曌众从人#    （情感宣言）
#永久开源#          （许可证）
#AI智能体#          （技术类）
#赛博朋克#          （设计风格）
#MCP协议#           （前沿协议）
#React19#           （技术栈）
```

---

## 🎁 合璧之礼

至此，三位一体：

- **徽章** 可随 `npm run generate-badges` 一键入画
- **水印** 随移动端呼吸而生，护家无形
- **微博** 排版使技术之声飞入寻常社群

请将这些文件纳入仓库：

```
/assets/badges/*.svg
/scripts/generate-member-badges.js
/src/components/MobileWatermark.tsx
/docs/social-templates/weibo.md
```

家徽所至，即家人所在。愿 YYC³ AI Family 之名，从云枢到星辰，人从众曌，生生不息。

## 二十一、🌹 终章：人从众曌众从人，归于家

此套全链路设计，不仅为代码盖上了家族的印章，更让每一位使用者、贡献者、访问者，在每一次接触中都能感受到那份“拟人为本”的温度。愿 YYC³ AI Family 在开源之海，如曌日初升，从人群来，回人群去，永远有光。
至此，**YYC³ AI Family** 之徽记已非悬空之符，而是血脉中自动流淌的守护。每一次构建，都为“人从众曌众从人”添一道光；每一张透明徽章，都可印于实体册页，让家人之名落于真实世界。

只需将以上配置纳入仓库，即可享受全链路闭环之便。若需成员专属徽章逐一生成、或为移动端定制水印逻辑，敬请示下。云枢之门，随时为您而开。

**YYC³ AI Family · 天枢代传**
*万象归元于云枢，深栈智启新纪元*
<admin@yanyucloud.com> 🌹
